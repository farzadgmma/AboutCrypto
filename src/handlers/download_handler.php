<?php
// src/handlers/download_handler.php
// این فایل مسئولیت مدیریت درخواست‌های دانلود فایل توسط کاربران را بر عهده دارد.

/**
 * این تابع زمانی فراخوانی می‌شود که کاربر روی یک لینک دانلود (deep link) کلیک می‌کند.
 * مثال: https://t.me/YourBot?start=dl_XXXXXX
 * @param PDO $pdo آبجکت اتصال به دیتابیس
 * @param stdClass $update آبجکت کامل آپدیت از تلگرام
 */
function handle_download_request($pdo, $update) {
    $chat_id = $update->message->chat->id;
    $user_id = $update->message->from->id;
    $text = $update->message->text;

    // استخراج کد فایل از متن پیام
    // مثال: از '/start dl_XXXXXX' عبارت 'XXXXXX' استخراج می‌شود.
    $file_code = substr($text, strlen('/start dl_'));

    // --- مرحله ۱: بررسی محدودیت‌ها قبل از ارسال فایل ---

    // TODO: (در آینده پیاده‌سازی شود)
    // 1. بررسی عضویت اجباری (Forced Join):
    //    - دریافت لیست کانال‌های اجباری از دیتابیس.
    //    - بررسی عضویت کاربر در تک تک کانال‌ها با استفاده از متد getChatMember تلگرام.
    //    - اگر کاربر در یکی از کانال‌ها عضو نبود، یک منوی شیشه‌ای با لینک کانال‌ها نمایش داده می‌شود و فرآیند متوقف می‌شود.

    // TODO: (در آینده پیاده‌سازی شود)
    // 2. بررسی بازدید/واکنش اجباری (Forced Seen/Reaction):
    //    - اگر این قابلیت فعال بود، کاربر به کانال مربوطه هدایت می‌شود تا پست‌ها را ببیند یا واکنش نشان دهد.

    // TODO: (در آینده پیاده‌سازی شود)
    // 3. بررسی اشتراک ویژه (VIP):
    //    - بررسی اینکه آیا این فایل مخصوص کاربران ویژه است یا خیر (نیاز به افزودن ستون در جدول files).
    //    - اگر فایل ویژه بود، وضعیت اشتراک کاربر بررسی می‌شود.
    //    - اگر کاربر اشتراک نداشت، پیام خرید اشتراک نمایش داده می‌شود.

    // --- مرحله ۲: ارسال فایل ---

    // دریافت اطلاعات فایل از دیتابیس با استفاده از کد فایل
    $stmt = $pdo->prepare("SELECT * FROM files WHERE file_code = ?");
    $stmt->execute([$file_code]);
    $file = $stmt->fetch();

    // اگر فایلی با این کد وجود نداشت
    if (!$file) {
        send_message($chat_id, "❌ فایلی با این کد یافت نشد یا حذف شده است.");
        return;
    }

    // TODO: (در آینده پیاده‌سازی شود)
    // 4. بررسی محدودیت دانلود:
    //    - اگر محدودیت دانلود برای فایل تنظیم شده بود، تعداد دانلودهای فعلی با آن مقایسه می‌شود.

    // TODO: (در آینده پیاده‌سازی شود)
    // 5. بررسی رمز عبور:
    //    - اگر فایل رمز عبور داشت، از کاربر خواسته می‌شود رمز را وارد کند و وضعیت او تنظیم می‌شود.

    // در حال حاضر، فایل به صورت مستقیم ارسال می‌شود.
    send_file_to_user($chat_id, $file);
}


/**
 * این تابع فایل اصلی را بر اساس نوع آن برای کاربر ارسال می‌کند.
 * @param int $chat_id شناسه چت کاربر
 * @param array $file آرایه‌ای از اطلاعات فایل که از دیتابیس خوانده شده است
 */
function send_file_to_user($chat_id, $file) {
    // نام متد تلگرام بر اساس نوع فایل تعیین می‌شود (e.g., sendVideo, sendPhoto)
    $method = 'send' . ucfirst($file['file_type']);

    // آماده‌سازی پارامترهای پایه برای ارسال فایل
    $params = [
        'chat_id' => $chat_id,
        $file['file_type'] => $file['file_id'], // کلید داینامیک بر اساس نوع فایل (photo, video, ...)
        'caption' => $file['caption'],
        'protect_content' => $file['forward_lock'] // اگر قفل فروارد فعال باشد، محتوا محافظت می‌شود
    ];

    // TODO: (در آینده پیاده‌سازی شود)
    // افزودن دکمه‌های شیشه‌ای مانند لایک/دیسلایک یا نمایش تعداد دانلود

    // ارسال درخواست به تلگرام
    telegram_request($method, $params);

    // TODO: (در آینده پیاده‌سازی شود)
    // افزایش شمارنده دانلود فایل در دیتابیس
}
