# راهنمای کامل نصب و اجرای ربات روی هاست سی‌پنل (cPanel)

این راهنما شما را قدم به قدم برای نصب و اجرای سورس ربات تلگرام روی هاست سی‌پنل راهنمایی می‌کند.

**نیازمندی‌ها:**
*   یک هاست با کنترل پنل cPanel
*   یک دامنه (یا زیردامنه) با گواهی SSL فعال (آدرس سایت با `https://` شروع شود)
*   توکن ربات تلگرام از BotFather

---

### مرحله ۱: ایجاد دیتابیس (پایگاه داده)

1.  وارد حساب کاربری cPanel خود شوید.
2.  در بخش **Databases**، روی گزینه‌ی **MySQL® Database Wizard** کلیک کنید.
3.  **Create A Database**: یک نام برای دیتابیس خود وارد کنید (مثلاً `mybot_db`) و روی **Next Step** کلیک کنید.
4.  **Create Database Users**: یک نام کاربری (username) و یک رمز عبور قوی برای کاربر دیتابیس ایجاد کنید. این اطلاعات را در جایی امن یادداشت کنید. سپس روی **Create User** کلیک کنید.
5.  **Add User to the Database**: در این مرحله، تیک گزینه‌ی **ALL PRIVILEGES** را بزنید تا تمام دسترسی‌ها به کاربر داده شود. سپس روی **Next Step** کلیک کنید.
6.  صفحه تأیید نمایش داده می‌شود. شما با موفقیت دیتابیس و کاربر آن را ساختید. اطلاعات زیر را حتماً یادداشت کنید:
    *   **نام دیتابیس** (مثلاً: `cpaneluser_mybot_db`)
    *   **نام کاربری دیتابیس** (مثلاً: `cpaneluser_mybot_user`)
    *   **رمز عبور دیتابیس**

---

### مرحله ۲: آپلود فایل‌های ربات

1.  در cPanel، به بخش **Files** بروید و روی **File Manager** کلیک کنید.
2.  به پوشه‌ی `public_html` (یا پوشه مربوط به دامنه/زیردامنه‌ای که می‌خواهید ربات روی آن اجرا شود) بروید.
3.  از منوی بالا، روی **Upload** کلیک کنید و تمام فایل‌های سورس ربات را در این مسیر آپلود کنید. فایل‌ها باید شامل موارد زیر باشند:
    *   `bot.php`
    *   `config.php`
    *   `utils.php`
    *   `jdf.php`
    *   `install.php`

---

### مرحله ۳: ویرایش فایل `config.php`

1.  در همان **File Manager**، روی فایل `config.php` کلیک راست کرده و گزینه‌ی **Edit** را انتخاب کنید.
2.  مقادیر زیر را با اطلاعات خودتان جایگزین کنید:
    *   `'YOUR_TELEGRAM_BOT_TOKEN'`: توکن ربات تلگرام خود را که از BotFather گرفته‌اید، اینجا قرار دهید.
    *   `'YOUR_DATABASE_NAME'`: نام کامل دیتابیسی که در مرحله ۱ ساختید را وارد کنید.
    *   `'YOUR_DATABASE_USERNAME'`: نام کاربری دیتابیسی که در مرحله ۱ ساختید را وارد کنید.
    *   `'YOUR_DATABASE_PASSWORD'`: رمز عبوری که برای کاربر دیتابیس تنظیم کردید را وارد کنید.
    *   `$admins`: شناسه عددی اکانت تلگرام خودتان را در این آرایه قرار دهید تا به عنوان ادمین اصلی شناخته شوید.
    *   `$web`: آدرس کامل دامنه‌ی خود را (با `https://`) وارد کنید.
3.  پس از اتمام ویرایش، روی **Save Changes** کلیک کنید.

---

### مرحله ۴: اجرای اسکریپت نصب

1.  مرورگر وب خود را باز کنید و آدرس فایل `install.php` را وارد کنید. آدرس به این شکل خواهد بود:
    ```
    https://yourdomain.com/install.php
    ```
    (به جای `yourdomain.com` آدرس دامنه خود را قرار دهید)

2.  اگر همه چیز را درست انجام داده باشید، پیامی مبنی بر موفقیت‌آمیز بودن نصب و ایجاد جداول دیتابیس مشاهده خواهید کرد.

3.  🔴 **مهم‌ترین قدم:** پس از مشاهده پیام موفقیت، **بلافاصله فایل `install.php` را از طریق File Manager هاست خود حذف کنید.** این کار برای امنیت ربات شما ضروری است.

---

### مرحله ۵: تنظیم Webhook (اتصال ربات به تلگرام)

1.  آخرین مرحله، معرفی آدرس فایل `bot.php` به تلگرام است. برای این کار، باید URL زیر را کپی کرده و اطلاعات خودتان را در آن جایگزین کنید:
    ```
    https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://yourdomain.com/bot.php
    ```
2.  **جایگزین‌ها:**
    *   `<TOKEN>`: توکن ربات خود را جایگزین کنید.
    *   `https://yourdomain.com/bot.php`: آدرس کامل فایل `bot.php` روی هاست خود را وارد کنید.

3.  آدرس تکمیل‌شده را در نوار آدرس مرورگر خود وارد کرده و کلید Enter را بزنید.

4.  اگر همه چیز درست باشد، تلگرام یک پیام JSON شبیه به این نمایش می‌دهد:
    ```json
    {"ok":true,"result":true,"description":"Webhook was set"}
    ```

**تمام!** ربات شما اکنون باید نصب و فعال باشد.
